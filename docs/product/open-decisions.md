# 돈독 미결정 질문 등록부

이 문서는 사용자 답변이 필요한 질문의 단일 원본이다. `권고`는 에이전트 의견일 뿐 결정이 아니며, 사용자가 답하기 전에는 구현 전제로 사용하지 않는다.

## 사용 규칙

- 상태: `OPEN` → `ANSWERED` 또는 명시적 `DEFERRED`
- 현재 작업의 결정 기한에 도달한 `OPEN` 질문만 사용자에게 묻는다.
- 한 번에 최대 3개를 묻고, 선택지·영향·권고를 함께 제시한다.
- 답변을 받으면 이 문서의 상태와 `docs/project-context.md`의 새 `D-*` 결정을 같은 변경에서 갱신한다.
- 질문과 무관한 안전하고 되돌릴 수 있는 구현 세부는 에이전트가 결정한다.

## 현재 우선순위

| Gate | 시점 | 질문 |
|---|---|---|
| G0 | 애플리케이션 scaffold 직전 | 없음 — Q-001 답변 완료 |
| G1 | DB/OpenAPI와 핵심 흐름 동결 전 | 없음 — Q-002~Q-011 답변 완료 |
| G2 | 해당 기능 구현 직전 | 없음 — Q-012~Q-023 답변 완료 |
| G3 | GitHub·QC·Mac mini 배포 전 | 없음 — Q-024~Q-030 답변 완료 |

## 확인된 문서 충돌

1. React SPA는 Q-001 답변으로 확정했다.
2. 개인 나가기는 Q-012 답변으로 제공하지 않기로 확정했다.
3. 이체 분류는 Q-007 답변으로 두지 않기로 확정했다.
4. 실시간 화면 반영은 Q-029 답변으로 MVP 범위에서 제외했다. 기존 SSE·outbox 전제는 REST 재조회와 version 충돌 처리로 교체한다.

## G0 — Scaffold

### Q-001 프론트엔드 프레임워크

- 상태: `ANSWERED`
- 답변: React SPA + Vite + TypeScript + React Router로 확정한다. 모바일 설치 경험을 위해 `vite-plugin-pwa`를 사용한다.
- PWA 범위: manifest·아이콘·standalone 설치와 버전된 정적 앱 셸만 service worker가 관리한다. API 응답은 network-only이고 오프라인 mutation queue는 만들지 않는다. 새 버전은 작성 중 draft 유실을 막기 위해 자동 강제 reload가 아니라 갱신 확인 UI를 제공한다.
- 근거: 홈 화면 설치와 앱 형태 실행은 반복 사용하는 가계부에 가치가 있고, React SPA와 Mac mini 정적 배포 구조에도 잘 맞는다. 오프라인 금융 데이터 캐시는 동시성·개인정보 비용이 이득보다 크다.
- 답변일: 2026-07-11
- 출처: Frontend, PM

## G1 — 데이터와 핵심 계약

### Q-002 다중 가계부의 MVP 화면 범위

- 상태: `ANSWERED`
- 답변: 한 사용자는 동시에 하나의 가계부에만 속할 수 있다. 가계부가 없을 때만 만들기·초대 수락이 가능하고, 이미 속해 있으면 다른 가계부 생성·참여를 거부한다. 가계부 이름, 이름 변경, switcher와 통합 홈은 만들지 않는다.
- 답변일: 2026-07-11
- 출처: PM, Backend, Frontend, UX

### Q-003 공개 핸들 생성 시점

- 상태: `ANSWERED`
- 답변: 공개 핸들은 만들지 않는다. 로그인 아이디는 인증 전용으로 숨기고 구성원 초대는 비밀 초대 URL과 직접 입력 코드로만 제공한다. 사용자 검색·부분 검색·공개 사용자 목록 API도 만들지 않는다.
- 답변일: 2026-07-11
- 출처: Frontend

### Q-004 가입 개방 범위

- 상태: `ANSWERED`
- 답변: 누구나 회원가입할 수 있는 공개 가입 서비스로 운영한다. 가계부 참여 권한은 별도 초대로 제어한다.
- 답변일: 2026-07-11
- 출처: Backend

### Q-005 초대 방식과 기본 정책

- 상태: `ANSWERED`
- 답변: 초대 URL과 직접 입력 코드를 모두 제공하되 두 원문을 분리한다. 직접 입력 코드는 숫자 6자리이고 URL에는 추측하기 어려운 별도 고강도 token을 넣는다. 로그인한 사용자가 현재 구성원을 확인하고 `참여하기`를 누르면 기존 멤버의 추가 승인 없이 즉시 참여하고 홈으로 이동한다. 참여 후 초대 화면은 없으며 추가 구성원 초대와 발급 내역 관리는 가계부 설정에서 제공한다. 가계부는 사용자 지정 이름 없이 계정당 하나의 공동 공간으로 취급한다.
- 정책: 초대는 발급 시점부터 7일간 유효하고 한 번만 사용할 수 있다. 6자리 코드는 기존 초대와 중복되지 않게 DB unique와 충돌 재시도로 발급하고, 직접 코드 확인은 사용자별 rate limit을 적용한다. 이미 가계부가 있는 사용자는 수락할 수 없으며, invitation 행 잠금과 전역 user membership unique로 동시 수락을 직렬화한다.
- 답변일: 2026-07-11
- 출처: PM, Backend, Frontend, UX

### Q-006 거래의 경제활동 주체 범위

- 상태: `ANSWERED`
- 답변: 모든 구성원이 모든 거래를 CRUD할 수 있다. 각 수입·지출·이체의 경제활동 주체 `performed_by`는 구성원 한 명이며 로그인한 본인을 기본 선택한다. 입력자는 다른 구성원 한 명으로 바꿀 수 있지만 `가계부 공동`이나 비율 분할 주체는 만들지 않는다. 작성자 `created_by`는 인증 세션에서 별도로 기록한다.
- 예시: A가 로그인해 B가 쓴 식비를 입력하면 `created_by=A`, `performed_by=B`다.
- 답변일: 2026-07-11
- 출처: 전 에이전트

### Q-007 이체 분류

- 상태: `ANSWERED`
- 답변: 이체에는 분류를 두지 않는다. 내용만 선택 입력하며 수입·지출 통계에서도 제외한다.
- 답변일: 2026-07-11
- 출처: Frontend가 UX–DB 충돌 발견

### Q-008 최초 잔액의 날짜 효력

- 상태: `ANSWERED`
- 답변: 2026-08-06 변경으로 사용자가 입력하는 금액은 선택한 `잔액 기준일` 시작 시점의 실제 `기준일 잔액`이다. 기준일 이전 거래도 허용하고 통계·원장 이력에는 그대로 남기되, 기준일 잔액에 이미 반영된 것으로 보아 현재 잔액에 다시 합산하지 않는다.
- 계산 계약: 현재 잔액은 `기준일 잔액 + 기준일 당일 이후 유효 posting`이다. API 필드명 `openedOn`·`openingBalanceWon`은 호환을 위해 유지한다. V18 이전 자산은 마이그레이션 직전 현재 잔액을 보존하도록 기존 opening posting과 기준일 이전 유효 posting을 새 기준일 잔액으로 합산해 이관한다. 기준일 이전 신용카드 구매 charge도 기준일 잔액에 흡수된 것으로 표시해 통계·이력에는 남기되 명세에 다시 합산하지 않는다.
- 답변일: 2026-07-11
- 변경일: 2026-08-06
- 출처: Database

### Q-009 자산 소유자 marker 변경과 과거 통계

- 상태: `ANSWERED`
- 답변: 자산 소유자를 다른 구성원으로 바꿀 때 해당 자산의 기존 수입·지출 거래 주체 `performed_by`도 새 소유자로 일괄 변경할지 `그렇다/아니다`를 묻는다. `아니다`면 자산 marker만 바꾸고 기존 거래는 유지하며, 거래별 수정은 언제나 가능하다.
- 범위: 통계에서 제외되는 이체와 카드 정산은 자동 변경하지 않는다. 자산을 `공동 소유`로 바꿀 때는 공동 거래 주체가 없으므로 일괄 변경을 제안하지 않고 기존 주체를 유지한다.
- 동시성: 영향 건수와 기간을 확인 화면에 표시하고 자산 version을 조건으로 한 트랜잭션에서 bulk update와 audit를 함께 기록한다.
- 답변일: 2026-07-11
- 출처: PM

### Q-010 카드 MVP의 결제 모델

- 상태: `ANSWERED`
- 답변: 카드 구매는 일시불과 할부를 지원한다. 서비스는 할부 이자를 계산하지 않고 실제 이자는 사용자가 별도 거래로 입력한다. 정규 결제일 전 선결제는 전액뿐 아니라 사용자가 지정한 부분 금액을 허용하고 한 명세에 여러 번 실행할 수 있다.
- 정산 계약: 각 할부 회차는 귀속 명세와 원금 금액을 가지며, 선결제와 정규 결제는 모두 통계 제외 이체다. 명세 잔액은 `청구 원금 - 성공한 결제 합계`로 계산하고 0 미만 결제는 허용하지 않는다.
- 답변일: 2026-07-11
- 출처: Database

### Q-011 카드 최초 음수 금액의 결제 예정 포함 여부

- 상태: `ANSWERED`
- 답변: 카드의 음수 기준일 잔액도 결제 예정에 포함한다. 잔액 기준일과 카드 마감·결제 설정으로 해당 명세와 결제 예정일을 계산하며, 계산된 결제일이 이미 지났다면 그 과거 예정일을 유지해 catch-up worker의 처리 대상으로 둔다.
- 기준일 이전 구매: 해당 구매는 카드 기준일 잔액에 이미 포함되므로 결제 예정에 중복 합산하지 않는다. 기준일 이후 실제 환불은 미결제 기준일 명세를 먼저 줄이고, 이미 결제한 부분은 실제 결제 계좌로 반환한다.
- 연결 결정: 정확한 마감 월·말일 계산은 Q-016에서 확정한다.
- 답변일: 2026-07-11
- 출처: Root 교차 검토

## G2 — 기능 구현

### Q-012 개별 멤버 나가기

- 상태: `ANSWERED`
- 답변: 개인 나가기 기능을 제공하지 않는다. 구성원은 초대로 추가만 가능하고, 데이터 제거는 기존의 가계부 전체 삭제만 제공한다.
- 답변일: 2026-07-11
- 출처: PM, Database, Backend, Frontend

### Q-013 나가는 멤버의 자산과 자동 정산

- 상태: `ANSWERED · NOT_APPLICABLE`
- 답변: Q-012에서 개인 나가기를 제공하지 않기로 했으므로 별도 처리가 필요 없다.
- 답변일: 2026-07-11
- 출처: PM, Database, Backend, UX

### Q-014 마지막 멤버와 재가입

- 상태: `ANSWERED · NOT_APPLICABLE`
- 답변: 나가기와 재가입 상태가 없다. 구성원은 초대로 추가만 가능하고, 가계부 전체 삭제 시 membership도 함께 삭제된다.
- 답변일: 2026-07-11
- 출처: Database, Backend

### Q-015 로그인 유지와 비밀번호 복구

- 상태: `ANSWERED`
- 답변: 서버 세션과 `Secure`·`HttpOnly`·`SameSite=Lax` session cookie를 사용한다. 2026-08-07 변경으로 session cookie에 최대 90일의 `Max-Age`를 부여해 브라우저·설치형 PWA 재실행 후에도 자동 로그인한다. 마지막 사용 후 30일 또는 생성 후 최대 90일에 만료하며 여러 기기 로그인을 허용한다. 현재 기기와 전체 기기 로그아웃을 제공한다.
- 이메일: 이메일 인증을 완료한 뒤 서비스를 사용할 수 있다. 비밀번호 재설정 token은 30분·1회용이며 성공 시 기존 모든 세션을 만료한다. 개발은 Mailpit, 운영은 환경변수로 교체 가능한 SMTP adapter를 사용한다.
- 답변일: 2026-07-11
- 변경일: 2026-08-07
- 출처: Backend

### Q-016 카드 결제 월과 달력일 계산

- 상태: `ANSWERED`
- 답변: 카드별 결제 월 offset은 0~2개월 중 선택하고, 해당 월에 결제일이 없으면 말일로 먼저 보정한다. 그 날짜가 토요일·일요일 또는 `Asia/Seoul` 기준 대한민국 공휴일·대체공휴일·임시공휴일이면 다음 한국 영업일까지 순연한다.
- 구현 기준: 공식 공휴일 데이터를 연도별로 갱신하고 계산된 `due_on`을 명세에 저장해 이후 달력·카드 설정 변경이 과거 명세를 바꾸지 않게 한다.
- 답변일: 2026-07-11
- 출처: Database, Backend

### Q-017 자동 카드 정산의 거래 주체

- 상태: `ANSWERED`
- 답변: 카드대금 정산은 수입·지출 경제활동이 아니라 결제 계좌에서 카드로 이동하는 통계 제외 자산 이동이므로 거래 주체를 두지 않는다. 카드 설정과 결제 기록에는 실제 돈이 빠지는 `settlement_asset_id`만 필수로 저장한다.
- 연결 결정: 자동 정산 transaction의 `performed_by`는 null이다. 수동 선결제는 실행한 사용자를 `created_by`로 감사 기록하지만 결제 자체의 경제활동 주체는 두지 않는다.
- 답변일: 2026-07-11
- 출처: Backend

### Q-018 카드 결제 계좌 잔액 부족

- 상태: `ANSWERED`
- 답변: 결제일에는 장부상 결제 계좌 잔액과 무관하게 남은 카드대금 전액을 기록하고 결제 계좌의 음수 잔액을 허용한다. 실제 은행 결제 성공 여부를 연동하는 서비스가 아니므로 잔액 부족 상태나 부분 정산을 만들지 않는다.
- UX: 결제 후 음수 잔액을 그대로 보여주고 사용자가 실제 상태에 맞게 거래를 보정한다.
- 답변일: 2026-07-11
- 출처: PM, Database, Backend, Frontend, UX

### Q-019 확정·결제된 카드 구매 수정·삭제

- 상태: `ANSWERED`
- 답변: `기록 정정`과 `환불 처리`를 별도 행동으로 분리한다. 기록 정정은 잘못 입력한 원거래·charge·명세·정산을 원 결제일 기준으로 소급 교정한다. 환불 처리는 원 구매와 결제 이력을 보존하고 실제 환불일의 지출 통계를 상쇄한다.
- 환불 posting: 아직 결제되지 않은 금액은 카드 부채를 줄이고, 이미 결제된 금액은 실제 결제에 사용된 계좌로 반환한다. 여러 선결제·정산 계좌가 있으면 최신 결제부터 원 결제 계좌별로 반환하며 카드·계좌·명세 변경은 한 트랜잭션으로 처리해 잔액 불일치를 허용하지 않는다.
- 통계와 구분: 월별 통계는 별도 상시 합계가 아니라 원거래를 기간 제한 SQL로 집계한다. `billed_amount_won`은 조회 성능 캐시가 아니라 확정된 카드 명세의 업무 snapshot이다.
- 구현: 정정·환불은 서버 preview→apply 계약을 사용하고, apply에서 구매 version과 명세·결제 상태를 다시 검사한다. 환불은 원 구매와 연결된 별도 기록으로 남기고, 같은 명세의 다른 구매를 포함한 유효 청구액보다 결제액이 커진 초과분만 최신 원 결제 계좌로 반환한다.
- 답변일: 2026-07-11
- 출처: Database, Backend

### Q-020 MVP 통계 범위

- 상태: `ANSWERED`
- 답변: MVP 통계는 선택 월 수입·지출·순액과 카테고리 비중, 선택 월이 속한 연도의 1~12월 수입·지출 합계를 제공한다. 일별 추이는 통계에서 제외하고 홈의 일별 원장에서 확인한다. 기본은 현재 구성원의 경제활동이며 `나 · 각 구성원 · 모두`를 상시 선택하고, 자산 소유자·카테고리는 선택형 세부 필터로 적용한다.
- 변경: 2026-08-03 사용자가 일별 추이 대신 해당 연도의 월별 수입·지출 bar graph를 선택했다. 2026-08-15 공동 전체 기본값을 현재 구성원 기본값과 구성원별 상시 선택으로 변경했다.
- 답변일: 2026-07-11
- 출처: PM, Frontend

### Q-021 PWA와 오프라인 범위

- 상태: `ANSWERED`
- 답변: 설치 가능한 PWA를 제공한다. manifest·아이콘·standalone 실행과 버전된 정적 앱 셸 캐시만 제공하고, API 응답·금융 데이터는 service worker가 캐시하지 않는다. 오프라인에서는 기존 화면과 작성 중 draft를 보존하되 조회 갱신과 쓰기를 차단하며 mutation queue를 만들지 않는다.
- 구현: React SPA + Vite에 `vite-plugin-pwa`를 사용하고, 새 버전은 사용자가 안전한 시점에 갱신하도록 prompt 방식으로 처리한다.
- 답변일: 2026-07-11
- 출처: Backend, Frontend, UX

### Q-022 동시 수정 충돌의 병합 수준

- 상태: `ANSWERED`
- 답변: 편집 화면을 열 때 읽은 entity version을 저장 요청에 포함한다. 저장 시 현재 version이 다르면 서버는 변경을 전혀 적용하지 않고 `412 VERSION_CONFLICT`를 반환한다.
- UX: 사용자의 draft는 보존하고 서버 최신값과 내 입력을 보여준다. 필드 자동 병합은 하지 않으며, 사용자가 최신값을 기준으로 다시 적용하거나 취소한다.
- 답변일: 2026-07-11
- 출처: Frontend

### Q-023 테마 정책

- 상태: `ANSWERED`
- 답변: 첫 방문은 OS의 light/dark 설정을 따르고, 사용자가 설정 화면에서 `기기 설정 · 라이트 · 다크` 중 하나를 선택하면 해당 기기의 로컬 저장소에 기억한다. 계정·DB 동기화 대상은 아니다. 로그인·온보딩·앱 상단과 sidebar에는 테마 전환 버튼을 반복 노출하지 않는다.
- 답변일: 2026-07-11
- 변경일: 2026-08-07
- 출처: UX

## G3 — 운영과 품질

### Q-024 Mac mini 접근 범위

- 상태: `ANSWERED`
- 답변: 개인 Mac mini에서 공개 도메인과 HTTPS로 인터넷에 공개한다. reverse proxy만 80/443을 노출하고 backend·PostgreSQL은 내부 network에 둔다.
- 보안 계약: 자동 TLS 갱신, secure/HttpOnly cookie, CSRF·인증/가입/초대/비밀번호 재설정 rate limit, 방화벽, 운영 secret 분리와 외부 노출 E2E를 배포 gate에 포함한다.
- 답변일: 2026-07-11
- 출처: PM, Backend, Frontend, QC

### Q-025 GitHub 저장소 공개 범위

- 상태: `ANSWERED`
- 답변: GitHub 저장소는 public으로 운영한다. secret scanning·push protection과 CI Gitleaks를 필수 방어로 사용하고, 실제 환경파일·인증서·백업·운영 데이터는 저장소와 CI artifact에 넣지 않는다.
- 답변일: 2026-07-11
- 출처: PM

### Q-026 백업·복구·삭제 데이터 보존

- 상태: `ANSWERED`
- 답변: 운영 PostgreSQL은 가계부 삭제 요청을 즉시 반영한다. 이미 생성된 암호화 백업에는 삭제 전 데이터가 rotation 전까지 최대 30일 남는 것을 허용하고, 만료된 백업은 자동 삭제한다.
- 백업 정책: 매일 1회 백업을 30일 보관하고, 주 1회 암호화한 백업을 Mac mini 밖의 별도 위치에 복제한다. 복구 시 삭제된 가계부가 다시 서비스되지 않도록 삭제 이후 기록을 재적용하거나 삭제 이후 백업을 선택하는 절차를 검증한다.
- 답변일: 2026-07-11
- 출처: PM, Database, Backend, QC

### Q-027 공식 지원 브라우저

- 상태: `ANSWERED`
- 답변: 권고안 B로 운영한다. Chromium 모바일·데스크톱, Edge, macOS/iOS Safari를 정식 지원하고 Firefox는 smoke 범위로 둔다. PR은 대표 Chromium, 릴리스는 WebKit과 실제 iPad/iPhone Safari 핵심 흐름을 차단 gate로 사용한다.
- 답변일: 2026-07-11
- 출처: QC

### Q-028 목표 데이터 규모와 응답 성능

- 상태: `ANSWERED`
- 답변: 한 가계부의 활성 자산은 최대 50개로 제한한다. 거래 건수에는 제품 상한을 두지 않고 필요한 범위만 cursor pagination으로 읽으며 프론트는 무한 스크롤·필요 시 가상화를 사용한다. 월별 달력·통계 쿼리는 요청 기간을 명시해 전체 원장을 한 번에 읽지 않는다.
- 구현 기준: 자산 50개 제한은 동시 생성에도 넘지 않도록 가계부 단위 잠금 안에서 서비스가 검사한다. JPA 연관관계의 lazy loading을 API pagination 대체로 사용하지 않고 목록 projection과 명시적 쿼리를 사용한다.
- 답변일: 2026-07-11
- 출처: Database, QC

### Q-029 동시 세션과 실시간 반영 목표

- 상태: `ANSWERED`
- 답변: MVP에는 SSE와 실시간 반영 지연 보장을 두지 않는다. 같은 row를 편집한 두 세션의 저장 경쟁만 version 기반 낙관적 잠금으로 처리하고, 다른 세션은 화면 진입·탭 재집중·사용자 새로고침 때 서버 데이터를 재조회한다.
- 구현 기준: command 성공 시 요청한 클라이언트의 TanStack Query 캐시만 갱신·무효화한다. sync outbox와 event replay는 만들지 않으며 실제 실시간 요구가 생기면 별도 결정으로 다시 도입한다.
- 답변일: 2026-07-11
- 출처: Backend, QC

### Q-030 CI·릴리스 gate와 실패 증거 보존

- 상태: `ANSWERED`
- 답변: B로 운영한다. PR은 build·unit·PostgreSQL integration·OpenAPI drift·핵심 Chromium E2E를 필수로 하고, 릴리스는 전체 핵심 E2E·이전 migration·동시성·백업 복원 검증까지 차단 gate로 둔다. 실패 seed는 24시간, 마스킹된 실패 artifact는 14일 보존한다.
- 답변일: 2026-07-11
- 출처: QC

### Q-031 신규 가계부 기본 자산과 자산 이름

- 상태: `ANSWERED`
- 답변: 신규 가계부는 0원의 `현금`, `계좌`, `신용카드`, `체크카드`를 생성자 소유로 하나씩 생성한다. 체크카드는 기본 계좌, 신용카드는 14일 정산·25일 다음 달 결제·기본 계좌·자동 정산 끔으로 시작한다. 자산 등록에는 `자산 이름 (선택)`을 노출하고 빈 경우에만 종류명과 번호 suffix를 사용한다. 기본 4개는 50개 한도에 포함하며 기존 가계부에 소급 생성하지 않는다.
- 답변일: 2026-07-15
- 출처: Product, Database, Backend, Frontend, QC

### Q-032 연결 계좌 등록 화면

- 상태: `ANSWERED`
- 답변: 신용카드·체크카드·적금 등록 중에는 연결 가능한 계좌가 이미 있어도 `계좌 추가` 행동을 항상 제공하고, 새 탭으로 이동하지 않고 현재 화면의 반응형 dialog에서 계좌를 등록한다. 모든 viewport에서 불필요하게 화면 높이를 채우지 않고 입력 콘텐츠 높이에 맞춘 중앙 dialog로 보여주며, 콘텐츠가 viewport보다 길 때만 최대 높이 안에서 스크롤한다. 계좌 dialog와 일반 자산 등록의 기준일 잔액은 미리 0으로 입력하지 않고 빈 값에 `0` placeholder만 제공하며, 빈 채로 저장하면 0원으로 정규화한다. 0원이 아닌 경우에만 사용자가 정수 원 금액을 입력한다. 부모 자산 draft를 보존하고, 계좌 생성 성공 시 기존 `POST /api/assets` 응답을 목록 cache에 반영해 새 계좌를 자동 선택한다. 오류 시 계좌 입력을 보존하며 취소·`Escape`·브라우저 뒤로 가기는 dialog만 닫고 trigger focus를 복원한다.
- 답변일: 2026-07-15
- 출처: Product, Frontend, UX/UI, QC

### Q-033 자산 현황 분류와 합계

- 상태: `ANSWERED`
- 답변: 자산 첫 화면은 순자산을 가장 큰 핵심 값으로 한 번만 보여주고 총자산·총부채를 계산 근거로 둔다. `CASH, OTHER, BANK, SAVINGS`는 화면에서 한 단어 `자금` 그룹으로 합치며, 그룹 현재 합계와 각 자산의 signed 현재 금액을 가장 오른쪽 한 열에 표시한다. `OVERDRAFT` 별도 종류는 2026-08-06 결정으로 제거하고 음수 잔액의 `BANK` 계좌를 마이너스 통장으로 본다. 그룹 `현재 합계` 금액은 16px을 유지하고 개별 자금의 `현재 자산` 금액은 14px로 한 단계 낮춰 합계와 항목의 위계를 구분한다. 음수 계좌 금액도 절댓값 `부채`로 분리하지 않고 `-금액`으로 표시한다. 카드는 현재 잔액·부채 대신 `이번 달 결제 금액 | 다음 달 결제 예정 금액` 두 열을 사용하고 카드 그룹·행에서 `부채` 표현을 쓰지 않는다. 전체 카드 그룹의 이번 달·다음 달 합계는 16px, 개별 카드 행의 같은 두 금액은 14px로 표시해 전체 합계를 한 단계 크게 읽히게 한다. 두 값은 `Asia/Seoul` 기준 각각 현재 달과 다음 달 due date에 속한 현재 미결제 명세 합계이며 서로 합산하지 않는다. 체크카드는 두 값 모두 `0원`으로 명시한다. 모든 그룹은 한 열의 고정 순서로 이어지고 빈 그룹은 숨기며 장식용 그래프나 구조적 카드 컨테이너를 만들지 않는다. 각 그룹 시작은 전체 폭 1px 기준선으로 구분하고 내부 구분선은 더 옅게 표현하되 배경·둥근 모서리·그림자를 추가하지 않는다. 모바일에서는 자산명과 필요한 metadata를 말줄임하지 않고 이름 아래 보조행으로 읽게 하며, 자금·투자·대출·보험의 단일 잔액은 이름 오른쪽에 두고 480px 미만에서만 카드 두 월 금액을 이름 아래에 둔다. 모바일 그룹 간격은 24px, iPad·데스크톱은 32px이고 소유자 subnav는 이름을 숨기지 않는 한 줄 가로 스크롤로 유지한다. 데스크톱에서는 목록 오른쪽 sticky rail에 같은 자산 요약을 배치한다. 기본 이름에는 종류를 반복하지 않고 custom 이름에만 종류를 표시하며, 소유자는 전체 보기에서만 `나`·`공동`·다른 구성원명으로 표시한다. 접근 가능한 링크 이름은 생략 여부와 관계없이 전체 종류·소유자를 유지한다.
- 답변일: 2026-08-07
- 출처: Product, Backend, Frontend, UX/UI, QC

### Q-034 분류 설정 메뉴와 표시 밀도

- 상태: `ANSWERED`
- 답변: 공동 수입·지출 분류는 가계부 설정의 독립 `분류 설정` 메뉴에서 관리한다. 수입·지출 전환과 각 분류를 버튼으로 제공하며, 분류마다 수정·삭제 도구를 반복하는 행 목록은 사용하지 않는다. 선택한 한 분류에만 연결 거래 수·이름 수정·조건부 삭제 관리영역을 보여주고 기본 `기타`에는 삭제 행동을 노출하지 않는다. 반응형 화면은 같은 버튼 DOM을 재배치하며 선택·draft·focus를 유지한다.
- 답변일: 2026-07-16
- 출처: Product, Frontend, UX/UI, QC

### Q-035 자산 현황의 사용자별 자산 보기

- 상태: `ANSWERED`
- 답변: 자산 현황에 `전체 · 공동 소유 · 각 구성원` 버튼형 소메뉴를 두고 소유 marker 기준으로 상단 합계와 아래 그룹·자산 행을 함께 필터링한다. 첫 진입은 현재 사용자의 자산이 기본이며 이름에 `(나)`를 붙이고 선택 하단선으로 현재 범위를 바로 알 수 있게 한다. 선택은 URL query에 유지하되 query가 없거나 잘못된 값이면 현재 사용자로 해석하고, 사용자가 고른 전체 보기는 `owner=all`로 명시한다. 활성 자산 수와 50개 한도는 언제나 가계부 전체 기준이다. 이 기능은 보기만 좁히며 모든 구성원의 동일한 자산 관리 권한을 바꾸지 않는다.
- 변경: 2026-08-06 사용자가 첫 진입의 기본 범위를 전체에서 현재 사용자 자산으로 변경했다.
- 답변일: 2026-07-16
- 출처: Product, Frontend, UX/UI, QC

### Q-036 삭제 가계부의 off-device 복구 내구성

- 상태: `OPEN`
- 결정 기한: weekly off-device 백업과 실제 disaster cutover 구현 전
- 배경: daily dump 안의 `audit_log`도 가계부와 함께 삭제되므로 삭제 전 backup만 복원하면 DB 자체에는 해당 가계부를 다시 제거할 근거가 없다. 로컬 restore drill의 선택 denylist는 코드 경로만 검증하며 실제 삭제 기록의 내구성을 만들지 않는다.
- 선택지 A: 삭제 확인 직후 DB 삭제보다 먼저 append-only 외부 tombstone을 동기적으로 내구화하고 이를 복구의 authoritative 삭제 목록으로 사용한다. 외부 기록이 불가능하면 삭제를 시작하지 않으며, 기록 뒤 로컬 트랜잭션이 실패해도 확인된 삭제 의도는 재시도·복구에서 최종 삭제로 수렴한다.
- 선택지 B: base backup과 연속 WAL/PITR를 Mac mini 밖에 보존하고 삭제가 포함된 복구 시점까지 replay한 결과만 서비스 승격 후보로 허용한다. 더 일반적인 시점 복구가 가능하지만 단일 개인 서버 운영 복잡도와 저장량이 커진다.
- 선택지 C: 외부에 마지막 삭제 완료 시각만 내구화하고 그 시각 이후 생성·복제된 backup만 복구 후보로 허용한다. 가장 단순하지만 삭제 직후 다음 backup 전 장애에는 사용할 수 있는 복구 지점이 없어진다.
- 권고: A. 현재 단일 Mac mini·낮은 사용자 규모에서 복구 목적을 삭제 재등장 방지로 좁히면서도 daily dump 구조를 유지할 수 있다.
- 필요한 사용자 결정: A/B/C
- 출처: PM, Database, Backend, QC

### Q-037 weekly off-device 백업 위치

- 상태: `OPEN`
- 결정 기한: weekly 암호화 복제 job 구현 전
- 전제: 목적지는 Mac mini의 내부 disk·같은 Docker VM·같은 전원 장애 경계 밖이어야 하고, 실제 dump와 개인정보를 public GitHub·CI artifact에 넣지 않는다.
- 선택지 A: versioning과 보존 정책을 지원하는 S3-compatible private object storage. 자동화·무결성·원격 보존에 유리하지만 월 비용과 자격 증명 관리가 필요하다.
- 선택지 B: 다른 장소의 개인 NAS 또는 항상 켜진 별도 장치. 인프라 통제권은 높지만 원격 접근·disk 상태·ransomware 분리·전원 장애를 직접 관리해야 한다.
- 선택지 C: 평소 분리해 두는 암호화 외장 disk에 주 1회 수동 복제한다. 단순하지만 사람의 실행 누락과 최신성 감시가 약하다.
- 권고: A. 작은 서비스에서도 Mac mini와 독립된 장애 경계를 가장 단순하게 자동화할 수 있다.
- 필요한 사용자 결정: A/B/C와 이미 사용할 목적지가 있으면 그 유형
- 출처: PM, Backend, QC

### Q-038 off-device 백업 암호화 키 보관

- 상태: `OPEN`
- 결정 기한: weekly 암호화 복제 job 구현 전
- 선택지 A: Mac mini에는 암호화용 public key만 두고 private 복호화 키는 password manager와 오프라인 복구 사본에 둔다. 서버 탈취 시 과거 백업 복호화 키가 같이 노출되지 않는다.
- 선택지 B: 대칭 repository 비밀번호를 Mac mini의 root/user 제한 secret과 password manager에 함께 둔다. 자동 복구는 단순하지만 서버 탈취 시 암호화 백업 접근 자격도 함께 노출될 수 있다.
- 선택지 C: 저장소 제공자의 server-side encryption만 사용한다. 전송·저장 보호는 되지만 저장소 계정 침해와 운영자 경계에서 client-side 암호화가 없어 권고하지 않는다.
- 권고: A. 백업 파일·GitHub·Mac mini의 동일 경로와 복호화 키를 분리한다는 기존 Q-026 계약에 가장 잘 맞는다.
- 필요한 사용자 결정: A/B/C
- 출처: PM, Backend, QC

## 사용자 질문에서 제외한 구현자 결정

다음은 제품 결과를 바꾸지 않는 한 에이전트가 결정하고 검증한다.

- URI·HTTP 상태코드·Java 클래스·패키지·파일명
- PostgreSQL 인덱스 이름, lock SQL, pagination cursor 형식
- worker 실행 분, request ID 내부 필드명
- package manager, form·chart library, query key factory
- 단순 spacing·아이콘·표현 차트 종류
- 단위 테스트 coverage 숫자와 trivial accessor 테스트

## 다시 묻지 않을 확정 사항

- N명 활성 멤버의 동일 권한과 전체 공유 편집
- 자산 소유자는 권한이 아닌 marker이며 공동 자산, 다른 구성원 소유 계좌와의 일반 이체, 교차 소유 정산이 가능
- 원화 정수·거래 날짜 `DATE`·기준일 잔액과 기준일 당일 이후 posting 합산 잔액
- 거래 주체와 작성자 분리
- 분류 삭제 시 같은 방향 `기타` remap, 자산 이력 존재 시 archive
- 카드 구매일 지출 반영, 카드 정산은 통계 제외
- 결제 완료 카드 구매의 감액·삭제는 원 결제 계좌로 환불하고 카드·계좌 원장을 함께 재조정
- 기본 통계는 현재 구성원의 경제활동이며 각 구성원·모두로 전환 가능
- REST command + version 기반 충돌 방지, 다른 세션은 진입·focus·수동 새로고침 때 재조회
- 알림 없음, PWA는 정적 앱 셸만 캐시하고 API 캐시·오프라인 쓰기는 구현하지 않음
- PostgreSQL·Spring Boot/JPA·shadcn/Tailwind·TanStack Query·Mac mini Docker Compose
- React SPA·Vite·React Router·`vite-plugin-pwa`
- 한 사용자는 하나의 가계부에만 참여, 개인 나가기 없음
- 공개 핸들·사용자 검색 없음, 초대 URL·코드만 제공
- 초대는 가계부 확인 후 즉시 참여, 7일 만료·1회 사용
- 누구나 회원가입 가능, 이메일 기반 비밀번호 재설정 제공
- 거래 주체는 구성원 한 명, 본인이 기본이며 다른 구성원 선택 가능; 작성자는 별도 기록
- 자산 소유자 변경 시 수입·지출 거래 주체의 선택적 일괄 변경, 이체·공동 소유 전환은 기존 주체 유지
- 카드 일시불·할부와 결제일 전 부분·복수 선결제 지원, 할부 이자는 사용자 별도 거래
- 이체 분류 없음, 기준일 잔액과 잔액 기준일을 자산 잔액의 anchor로 사용
- 모바일·iPad 세로/가로·데스크톱의 세분화 반응형 지원과 회전 중 UI 상태 보존
- 카드 결제일은 선택한 결제 월의 달력일을 말일 보정한 뒤 비영업일이면 다음 한국 영업일로 순연
- 카드 정산은 경제활동 주체 없이 결제 계좌만 기록하고 잔액 부족에도 전액 기록·음수 허용
- MVP 통계 A, OS 기본+기기별 테마 선택, 활성 자산 50개와 거래 cursor pagination
- 공개 도메인+HTTPS Mac mini 배포, public GitHub 저장소, 브라우저 지원 B, CI·릴리스 gate B
- 운영 PostgreSQL 삭제는 즉시 반영하고 암호화 백업의 삭제 전 데이터는 최대 30일 보존 후 자동 만료
- 모든 자산 신규 등록은 종류·선택 이름·기준일 잔액·잔액 기준일을 받고, 이름을 비우면 종류 기본명을 사용하며 소유·메모는 되돌릴 수 있는 기본값으로 저장한 뒤 상세에서 수정
- 신용카드 신규 등록은 정산일·결제 월/일·결제 계좌를 필수로 받고 자동 정산은 상세에서 선택
- 체크카드는 결제 계좌를 필수로 저장한다. 적금 자동이체는 선택이며 사용하면 계좌·일을 모두 저장하고 사용하지 않으면 설정 없이 등록한다. 적금 자동 실행은 세부 정책 확정 전 생성하지 않음
- 자산 종류는 고정 시스템 종류만 제공하고 사용자 정의 종류를 만들지 않음; `기타`는 현금과 같은 일반 현금성 자산, `저축` 표시명은 `적금`
- 신규 가계부는 0원·생성자 소유의 현금·계좌·신용카드·체크카드를 하나씩 생성하고 각 카드를 기본 계좌에 연결
- 연결 계좌가 없는 카드·적금 등록은 새 탭 대신 부모 draft를 보존하는 반응형 계좌 등록 dialog를 사용하고 생성 성공 즉시 새 계좌를 선택
- 연결 계좌 후보가 있어도 카드·체크카드·적금 설정에서 계좌 추가 dialog를 항상 제공
- 자산 화면은 총자산·총부채·순자산, 이번 달·다음 달 카드 결제 금액과 자금·카드·투자·대출·보험 그룹 현황을 제공
- 사용자 수입·지출은 거래별 집계 제외를 선택할 수 있고, 자산 잔액은 바뀌지만 월간 달력·통계에는 포함되지 않으며 원장에는 `집계 제외`로 남음

### D-046 적금 자동이체 선택 설정

적금 등록·수정에서 자동이체는 선택 사항이다. 자동이체를 사용하지 않으면 `savingsSettings = null`로 저장하고 `savings_setting` 행을 만들지 않는다. 사용하면 출금 계좌와 달력일을 같은 설정으로 모두 입력해야 하며 부분 설정은 허용하지 않는다. 기존 설정이 있는 적금에서 자동이체를 끄면 설정 행을 삭제하되 적금 자산·거래·posting·잔액은 유지한다. 설정 자체로 자동 거래는 생성하지 않는다.

### D-047 사용자 선택 수입·지출 집계 제외

모든 사용자 수입·지출은 거래별로 집계 제외를 선택할 수 있다. 제외해도 자산 posting과 카드 구매·환불 원장은 그대로 반영하고 월간 달력과 통계 합계·분류·연간 흐름에서만 제외한다. 제외된 기록은 일별 원장과 상세에서 `집계 제외`로 식별하고 일반 거래 수정 또는 카드 구매 정정으로 다시 포함할 수 있다. 카드 환불은 원 구매의 선택을 기본으로 이어받되 환불 거래별로 바꿀 수 있고, 기존 거래와 필드를 보내지 않는 이전 클라이언트는 포함이 기본이다.
