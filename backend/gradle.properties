# Gradle's native macOS file watcher can block while snapshotting this workspace.
# Incremental task inputs remain enabled; only long-lived filesystem watching is disabled.
org.gradle.vfs.watch=false
